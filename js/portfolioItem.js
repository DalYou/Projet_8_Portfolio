function portfolioModal (selector) {
    const element = document.querySelector(selector)
    const portfolioItems = element.querySelectorAll(".portfolio-item")

    for(let i=0;i<portfolioItems.length;i++){
        const portfolioItem = porfolioItems[i];
        portfolioItem.addEventListener("click",e=>{
            
        } )
    }
}